# - platform: template
  # sensors:
  # bryan_phone:
    # value_template: 'Unknown'
    # friendly_name: "Bryan's Phone"

    # bryan_tablet:
      # value_template: 'Unknown'
      # friendly_name: "Bryan's Tablet"

    # bryan_phone_battery:
      # value_template: '{% if states.sensor.bryan_phone %}{{ states.sensor.bryan_phone.attributes.battery_level }}{% else %}Unknown{% endif %}'
      # icon_template: '{% if states.sensor.bryan_phone %}{{ states.sensor.bryan_phone.attributes.icon }}{% else %}mdi:battery-unknown{% endif %}'



    # bryan_phone_view:
      # value_template: '{% if states.sensor.bryan_phone %}{{ states.sensor.bryan_phone.state }}{% else %}Unknown{% endif %}'
      # friendly_name: "Bryan's Phone"
      # icon_template: '{% if states.sensor.bryan_phone %}{{ states.sensor.bryan_phone.attributes.icon }}{% else %}mdi:battery-unknown{% endif %}'

